# Datasets & archives (huge — never commit)
*.zip
data/
sample/

# Caches & artifacts
*.npz
__pycache__/
*.pyc

# Environment
.venv/

# Intermediate model checkpoints (keep only the chosen embed2_best.pt + ONNX)
embed_best.pt
embed3_best.pt
embed224_best.pt
train_embed224.py
build_imgcache_color224.py
